<!DOCTYPE html>
<%- partial('_partial/head') %>
<html>

<body>
  <%- partial('_partial/import', { resource: theme.import.body.before }) %>
  <div class="container">
    <%- partial('_partial/header') %>

    <% if (is_home()) { %>
    <div class="intro-card">
      <ul>
        <li>Not a typical blog — more like a bunch of personal notes written in blog style.</li>
        <li>Mostly written with AI assistance, guided by my input.</li>
        <li>Primarily for my own reference, and occasionally for sharing with folks I personally know or have worked with.</li>
        <li>I don't care about the writing style or perfect wording, just aiming to note down the information that matters.</li>
      </ul>
    </div>
    <% } %>
    <div class="main-wrap">
      <main class="main-width">
        <% if(is_page() || is_post()){ %>
        <h1 class="page-title"><%= page.title || _p('post.title') %></h1>
        <% } %>
        <%- body %>
      </main>
      <%- partial('_partial/aside') %>
    </div>
    <%- partial('_partial/pagination') %>
    <%- partial('_partial/footer') %>
  </div>
  <%- partial('_partial/import', { resource: theme.import.body.after }) %>
</body>

</html>