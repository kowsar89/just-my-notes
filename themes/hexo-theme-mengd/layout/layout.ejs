<!DOCTYPE html>
<%- partial('_partial/head') %>
<html>

<body>
  <%- partial('_partial/import', { resource: theme.import.body.before }) %>
  <div class="container">
    <%- partial('_partial/header') %>

    <% if (is_home()) { %>
    <div class="intro-card">
      <p>Not a typical blog — more like a bunch of personal notes written in blog style. Mostly written with the help of AI based on my input, primarily for my own reference, and occasionally for sharing with folks I personally know or have worked with.</p>
    </div>
    <% } %>
    <div class="main-wrap">
      <main class="main-width">
        <% if(is_page() || is_post()){ %>
        <h1 class="page-title"><%= page.title || _p('post.title') %></h1>
        <% } %>
        <%- body %>
      </main>
      <%- partial('_partial/aside') %>
    </div>
    <%- partial('_partial/pagination') %>
    <%- partial('_partial/footer') %>
  </div>
  <%- partial('_partial/import', { resource: theme.import.body.after }) %>
</body>

</html>